apiVersion: v1
kind: Pod
metadata:
  name: simple-webapp
  labels:
    name: app
spec:
  containers:
    - name: web-application
      image: nginx
      volumeMounts:
        - name: shared-logs
          mountPath: /var/log/nginx
    - name: sidecar-container
      image: busybox
      command: ["sh","-c","while true; do cat /var/log/nginx/access.log; sleep 30; done"]
      volumeMounts:
        - name: shared-logs
          mountPaths: /var/log/nginx
  volumes:
    - name: shared-logs
      emptyDir: {}